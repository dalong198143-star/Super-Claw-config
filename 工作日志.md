# 📋 工作日志 - 短平快(WisdomFlow)学习变现平台

## 📅 2026年5月7日 - 文生图工具全面重构 + DeepSeek/Replicate API 集成（v1.10.0）

### ✅ 已完成任务

#### 1. 后端：Replicate 文生图 API 集成

**目标**: 替换假的 picsum.photos 占位服务，接入真实 AI 图像生成

**新建文件**:
- `server/services/replicate.js` — Replicate API 客户端（createPrediction + getPrediction）
- 使用 `black-forest-labs/flux-schnell` 模型（免费快速，1-3秒生成）
- 支持参数：prompt、negative_prompt、width、height、steps、guidance、seed

**新增路由**:
- `POST /api/replicate/generate` — 发起图像生成
- `GET /api/replicate/prediction/:id` — 轮询生成状态

**降级策略**: Token 未配置时返回 HTTP 503 + 明确提示

---

#### 2. 后端：DeepSeek API 集成

**目标**: 接入 DeepSeek 实现提示词优化和 AI 助手对话

**新建文件**:
- `server/services/deepseek.js` — DeepSeek API 客户端
- `optimizePrompt()` — 将中文描述优化为高质量英文绘画提示词
- `chat()` — 通用对话接口（兼容 OpenAI 格式）

**新增路由**:
- `POST /api/deepseek/optimize-prompt` — AI 优化提示词
- `POST /api/deepseek/chat` — AI 助手对话

---

#### 3. 配置更新

**修改文件**:
| 文件 | 改动 |
|------|------|
| `server/config.js` | +REPLICATE_API_TOKEN、REPLICATE_MODEL、DEEPSEEK_API_KEY、DEEPSEEK_MODEL |
| `.env.example` | +REPLICATE_API_TOKEN、DEEPSEEK_API_KEY 环境变量 |

---

#### 4. 前端：useTextToImage 自定义 Hook

**新建文件**: `client/src/hooks/useTextToImage.js`

**封装内容**:
- 提示词状态（prompt、negativePrompt）
- 风格选择（8种风格，从 /api/image-styles 加载）
- 参数配置（width、height、steps、guidance、seed）
- 生成状态（isGenerating、progress、generatedImage、error）
- AI 优化提示词（调用 DeepSeek）
- Replicate 轮询生成（1秒轮询，120秒超时）

---

#### 5. 前端：TextToImageTool 组件全面重写

**重写文件**: `client/src/components/tools/TextToImageTool.jsx`
- **重构前**: 52行，仅实现 Step 1/3 的输入界面
- **重构后**: 282行，与 LinearWorkflow 4步骤系统完整配合

**4步骤设计**:
| 步骤 | 名称 | 功能 |
|------|------|------|
| Step 1 | 输入提示词 | 正向提示词 textarea + 负向提示词 input + AI 优化按钮 |
| Step 2 | 选择风格 | 8格风格卡片网格（2行4列），选中高亮+✓标记 |
| Step 3 | 配置参数 | 图片宽高/推理步数/引导系数 range slider + 随机种子 |
| Step 4 | 生成结果 | 开始生成按钮 → 进度条轮询 → 图片预览 + 下载/重新生成 |

**新建文件**: `client/src/components/tools/TextToImageTool.css` — 完整样式（含响应式）

---

#### 6. 前端：AI Assistant 升级

**修改文件**: `client/src/components/shared/AIAssistant.jsx`

**改动**:
- 新增 `aiProvider` prop，显示当前 AI 提供者（DeepSeek-Chat / Ollama 本地 / 云端辅助）
- 模型选择器仅在 Ollama 模式下显示
- 移除 emoji，精简按钮文案

---

#### 7. 前端：App.jsx 集成

**修改文件**: `client/src/App.jsx`

**移除**: 5个文生图相关的 useState（imagePrompt、selectedStyle、imageStyles、generatedImage、imageGenerating）
**新增**:
- `useTextToImage()` hook 调用
- `detectAIProvider()` — AI 检测优先级: DeepSeek → Ollama → Cloud
- `callDeepSeek()` / `askAI()` — 按 AI 提供者路由对话

---

#### 8. 测试更新

**修改文件**:
| 文件 | 改动 |
|------|------|
| `src/__tests__/TextToImageTool.test.jsx` | 重写为 23 个测试，覆盖全部 4 个步骤 |
| `src/__tests__/components/AIAssistant.test.jsx` | 适配 aiProvider prop |
| `src/__tests__/setup.js` | 补充 window.matchMedia mock |

---

#### 9. 构建与测试验证

**生产构建**:
```
vite v8.0.10 building client environment for production...
✓ 50 modules transformed.
✓ built in 385ms
```

**测试结果**:
```
Test Suites: 25 passed, 25 total
Tests:       5 skipped, 270 passed, 275 total
```

**变更统计**:
| 类别 | 数量 |
|------|------|
| 新建文件 | 5 |
| 修改文件 | 8 |
| 新增 API 端点 | 4 |
| 新增测试 | 23→270 (+13，含修复) |

---

### 📋 使用前配置

在 `.env` 中设置以下 API Key：
- `REPLICATE_API_TOKEN` — 注册 https://replicate.com 获取
- `DEEPSEEK_API_KEY` — 注册 https://platform.deepseek.com 获取

---

## 📅 2026年5月6日 - 问题修复与全面测试（v1.9.3）

### ✅ 已完成任务

#### 1. 版本号统一更新

**修复内容**:
- ✅ `Header.jsx` 版本号从 `v1.8.0` 更新为 `v1.9.3`
- ✅ `client/package.json` 版本号确认为 `1.9.3`
- ✅ `server/package.json` 版本号确认为 `1.9.3`

#### 2. 测试失败问题修复

**修复的问题**:
| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 版本号不匹配 | `Header.test.jsx` | 更新测试期望值为 `v1.9.3` |
| CSS导入路径错误 | `WorkflowModeSwitcher.jsx` | 从 `../../styles/` 改为 `./` |
| 测试用例与实现不匹配 | `WorkflowModeSwitcher.test.jsx` | 更新描述文本和CSS变量名 |

#### 3. 构建验证

```
vite v8.0.10 building client environment for production...
✓ 48 modules transformed.
dist/index.html                   0.45 kB │ gzip:  0.29 kB
dist/assets/index-BxH2BSUN.css   43.44 kB │ gzip:  7.68 kB
dist/assets/index-kwpPbl-b.js   250.04 kB │ gzip: 76.28 kB
✓ built in 329ms
```

#### 4. 测试结果

```
Test Suites: 25 passed, 25 total
Tests:       5 skipped, 257 passed, 262 total
Time:        23.6 s
```

**测试覆盖统计**:
| 类别 | 数量 |
|------|------|
| 测试套件 | 25 |
| 通过测试 | 257 |
| 跳过测试 | 5 |
| 总测试数 | 262 |

---

## 📅 2026年5月6日 - 第二阶段：工作流功能完善完成（v1.9.2）

### ✅ 已完成任务

#### 1. 专家模式连线编辑功能

**目标**: 实现节点之间的可视化连线，支持工作流图构建

**完成的功能**:
- ✅ **连线模式切换**: 工具栏新增"连线模式"按钮
- ✅ **点击创建连线**: 在连线模式下点击两个节点即可创建连线
- ✅ **SVG贝塞尔曲线**: 使用平滑曲线连接节点
- ✅ **箭头标记**: 动态箭头指示连线方向
- ✅ **连线删除**: 点击连线上的×按钮删除连线
- ✅ **动画效果**: 虚线流动动画+节点脉冲高亮
- ✅ **交互优化**: 连线模式下禁用拖拽，悬停提示

**技术实现**:
- SVG `<path>` 绘制贝塞尔曲线
- CSS `stroke-dasharray` + `animation` 实现流动效果
- Zustand状态管理连线数据
- React hooks管理UI交互状态

**文件修改**:
- [`CanvasWorkflow.jsx`](d:\maozhua\xuexi编程基础\client\src\components\workflow\CanvasWorkflow.jsx) - +110行
- [`CanvasWorkflow.css`](d:\maozhua\xuexi编程基础\client\src\components\workflow\CanvasWorkflow.css) - +80行

---

#### 2. 智能推荐AI分析优化

**目标**: 提升推荐算法的准确性和智能化程度

**完成的改进**:
- ✅ **智能评分算法**:
  - 关键词匹配: 1分/词
  - 标题词匹配: 2分/词（权重更高）
  - 描述词匹配: 1.5分/词
  - 按总分排序返回

- ✅ **模板库扩展**:
  - 新增"AI绘画创作"模板（第6个模板）
  - 每个模板增加keywords字段

- ✅ **使用历史记录**:
  - 自动保存到localStorage
  - 记录使用时间戳
  - 最多保留20条历史